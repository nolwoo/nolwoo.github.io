# 범일금고 — 검색 노출 작업 핸드오프 (2026-08-29 시작, 2026-08-31 갱신)

다른 세션에서 이어서 진행할 때 참고. 프로젝트: `web-builder/bumil-safe` (Next.js, `bumilsafe-ydp.co.kr`).
**목표: 홈페이지 검색 노출 극대화 → 카카오톡·전화 상담 전환.**

## ① 네이버 서치어드바이저 — 완료 (2026-08-31)

- 소유확인 값 `b64cc2882d96b57be7c859aa4f7230596d1a3a6f` (HTML 태그) → `.env.local` + Vercel env + 재배포 → 실서버 `<head>` 반영 확인
- **소유확인 통과 완료**
- **sitemap.xml 제출 완료** (`요청 → 사이트맵 제출`, URL 46개 = 정적 6 + 제품 40)
- 남음: `요청 → 웹 페이지 수집`에서 홈 URL 수동 수집 요청(선택), 며칠 뒤 색인 현황 확인
- (참고: `.claude/launch.json`에 `autoPort: true` 추가 — 포트 3000 충돌 회피용)

## ② 네이버 스마트플레이스 — 진행 중 (2026-08-31)

- "범일금고 영등포대리점" 이미 등록돼 있음 (영등포로 164 / 쇼핑,유통>금고). **주인변경 불필요** —
  사용자가 소유 계정(`safecho`) 보유. new.smartplace.naver.com 에서 바로 관리 중.
- 기본정보 입력 중: 업종 "금고", 대표사진(매장 외관) 등록됨.
- **NAP는 홈페이지와 일치시킬 것** (`lib/site.ts` 기준): 상호 "범일금고 영등포대리점" /
  02-2632-6070 / 서울 영등포구 영등포로 164 / 홈페이지 https://bumilsafe-ydp.co.kr /
  평일 09:00–18:00.
- 제품 사진 7장 다운로드해 `~/Desktop/범일금고-플레이스사진/` 에 둠 (디자인·지문인식·기본형 골고루,
  본사 shop-phinf CDN 컷). 대표사진은 매장 외관 유지, 제품 사진은 뒤에 추가.
- 남음: 홈페이지 URL·상세설명 칸 확인 → 저장 → 네이버 검수 통과 대기(하루이틀) → 영업시간·부가정보 탭.

## ③ 아직 안 한 검색 등록 (사용자, 콘솔 작업)

- **구글 비즈니스 프로필**: 등록/소유 여부 미확인. 안 돼 있으면 최우선 (구글 지도 "영등포 금고" 로컬팩).
- **빙 웹마스터도구**: 미등록. 구글 서치콘솔에서 사이트 가져오기 1클릭. (ChatGPT 검색·코파일럿)
- **다음 검색등록**: register.search.daum.net, 미등록.

## ④ 홈페이지 코드 개선 (Claude가) — 노출 + 전환

기반은 돼 있음: `app/sitemap.ts`(46 URL)·`app/robots.ts`·`app/layout.tsx` LocalBusiness(Store) JSON-LD·
`app/service/page.tsx` FAQ JSON-LD·GA4(G-Q58KQX2ZVV, `@next/third-parties`).

완료 (2026-08-31, 커밋됨):
- [x] 제품 상세 40p **Product JSON-LD** (brand·offers KRW·aggregateRating) + **BreadcrumbList JSON-LD**
      — `app/products/[id]/page.tsx`
- [x] **상담 전환 GA4 추적** — `lib/track.ts`의 `trackConsult(method, location)` → `consult_click` 이벤트.
      `ConsultButtons`(카톡·전화)·`TopBanner`·`Footer`(신규 `components/PhoneLink.tsx`) 6개 접점 전부 연결.
      method=kakao|phone, location=header|mobile-drawer|topbanner|footer|page. dev에서 발화 확인.
- [x] 제품 상세 **OG 이미지 = 제품 사진** (`generateMetadata`의 openGraph.images = p.img)

남은 것:
- [ ] GA4 콘솔에서 `consult_click`을 **전환(주요 이벤트)** 으로 표시 → 노출→상담 측정 (사용자)
- [ ] 페이지별 title/description 점검 (영등포+제품유형 키워드) — 미착수
- [ ] Google Rich Results Test로 Product/FAQ 구조화 데이터 검증 (배포 후)

## ⑤ 지속 유입 (사용자/사장님)

- 네이버 블로그 개설 → 롱테일 글(가정용 금고 고르는 법 / 지문인식 금고 후기 / 사무실 금고 설치 사례),
  각 글에서 홈페이지·상담 링크. 네이버는 블로그 탭 노출 비중 큼.
- 스마트플레이스 소식·리뷰 답글 관리.

## 상태 요약 (2026-08-31)

| 채널 | 상태 |
|---|---|
| 구글 서치콘솔 | 완료 (소유확인·sitemap) |
| 네이버 서치어드바이저 | 완료 (소유확인·sitemap) |
| 네이버 스마트플레이스 | 진행 중 (기본정보 입력·저장·검수 대기) |
| 구글 비즈니스 프로필 | 미확인 → ③ |
| 빙 / 다음 | 미등록 → ③ |
| 카카오 알림톡(SOLAPI) | 안 함 (카톡 채널 채팅으로 충분) |
| 홈페이지 코드 SEO/전환 개선 | ④ 대기 |

관련 메모리: `project-bumil-safe-seo-launch` (web-builder auto-memory)
